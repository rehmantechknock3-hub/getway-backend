import { View, Text } from "react-native";

export default function ScheduleScreen() {
  return (
    <View className="flex-1 bg-gray-50 items-center justify-center">
      <Text className="text-xl font-semibold text-gray-900">Schedule</Text>
      <Text className="text-gray-500 mt-2">Availability management — M3</Text>
    </View>
  );
}
